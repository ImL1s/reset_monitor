# Spike B — OpenAIDevs auxiliary poll

**Evidence:** `AUTHOR_ALLOWLIST` has openaidevs with placeholder user id; `MONITORED_ACCOUNTS` is Tibo + ClaudeDevs only.

## Requirements
- Real X user id before enable.
- Same strict gate as Codex; marketing posts must not green.
- userId enforcement (plan 010) first.

## Go/no-go
Blocked until real id + fixtures + no false-green on product announcements.
